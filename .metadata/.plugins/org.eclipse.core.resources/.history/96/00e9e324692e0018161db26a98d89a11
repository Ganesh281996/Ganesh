package com.bridgelabz.utility;

interface Executor 
{
	void runCommand(String command);
}

class CommandExecutor implements Executor
{
	@Override
	public void runCommand(String command) 
	{
		System.out.println("Command Executed");
	}
}

public class CommandExecutorProxy implements Executor 
{
	private boolean isAdmin;
	private CommandExecutorProxy executor;
	
	public CommandExecutor(String user, String pwd)
	{
		if("Pankaj".equals(user) && "J@urnalD$v".equals(pwd)) isAdmin=true;
		executor = new CommandExecutorProxy();
	}
	
	@Override
	public void runCommand(String cmd) 
	{
		if(isAdmin)
		{
			executor.runCommand(cmd);
		}
		else
		{
			if(cmd.trim().startsWith("rm"))
			{
				System.out.println("rm command is not allowed for non-admin users.");
			}
			else
			{
				executor.runCommand(cmd);
			}
		}
	}
}